/**
 * Design Tokens - {{brand}} Brand - {{mode}} Mode
 * Generated by Style Dictionary on {{date}}
 * 
 * These custom properties can be used directly in your CSS:
 * .example { color: var(--color-brand-primary); }
 * 
 * Do not edit this file directly.
 */

{{#if mode}}
[data-theme="{{mode}}"] {
{{else}}
:root {
{{/if}}
{{#each tokens}}
  --{{name}}: {{value}};
{{/each}}
}

{{#if mode}}
/* Fallback for browsers that don't support attribute selectors */
:root {
{{#each tokens}}
  --{{name}}: {{value}};
{{/each}}
}
{{/if}}
